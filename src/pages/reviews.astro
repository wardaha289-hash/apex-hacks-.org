---
import BaseLayout from '../layouts/BaseLayout.astro'
import { ReviewsPage } from '../views/ReviewsPage'
import { SEO, SITE_URL } from '../data/site'
import { productReviewsJsonLd } from '../lib/seo'

const jsonLdExtra = [
  productReviewsJsonLd(),
  {
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: `${SITE_URL}/` },
      { '@type': 'ListItem', position: 2, name: 'Reviews', item: `${SITE_URL}/reviews` },
    ],
  },
]
---

<BaseLayout seo={SEO.reviews} jsonLdExtra={jsonLdExtra}>
  <ReviewsPage client:visible />
</BaseLayout>

